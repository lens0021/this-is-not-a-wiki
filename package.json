{
  "private": true,
  "scripts": {
    "test": "yarn run lint",
    "lint": "yarn run prettier --check .",
    "fix": "yarn run prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  },
  "prettier": {
    "singleQuote": true
  }
}
