{
  "private": true,
  "scripts": {
    "test": "yarn run lint",
    "lint": "yarn run prettier --check .",
    "fix": "yarn run prettier --write ."
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^1.2.0",
    "prettier": "^2.5.1"
  },
  "prettier": {
    "singleQuote": true
  }
}
